{
    "name": "ci-cd-demo",
    "version": "1.0.0",
    "scripts": {
      "test": "jest",
      "build": "react-scripts build"  // Change this if you're not using React
    },
    "devDependencies": {
      "jest": "^29.7.0",
      "jest-environment-jsdom": "^28.1.0"
    },
    "jest": {
      "testEnvironment": "jest-environment-jsdom"
    }
  }
  
  